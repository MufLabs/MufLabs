import { AgentFactory } from "../core";
import { MarkdownAgentLoader } from "../loaders";
import { AgentRegistry } from "../registry";
import { AgentRuntime } from "../runtime";
import { ManagerState } from "./ManagerState";
export class AgentManager {
    options;
    /**
     * TEMPORARY DEBUG PROPERTY
     * Remove after diagnostics.
     */
    __debug = "AgentManager MODIFICADO";
    loader = new MarkdownAgentLoader();
    registry = new AgentRegistry();
    runtime;
    state = ManagerState.Idle;
    constructor(options = {}) {
        this.options = options;
        this.runtime = new AgentRuntime(this.registry, this.options.runtime);
    }
    async initialize() {
        if (this.state !== ManagerState.Idle) {
            return;
        }
        this.state = ManagerState.Initializing;
        const definitions = await this.loader.load();
        const agents = AgentFactory.createMany(definitions);
        this.registry.registerMany(agents);
        this.state = ManagerState.Ready;
    }
    /**
     * Resolves the most appropriate agent for a command.
     */
    resolveByCommand(command) {
        const normalized = command
            .trim()
            .toLowerCase();
        console.log();
        console.log("========== resolveByCommand ==========");
        console.log("Command:", command);
        console.log("Normalized:", normalized);
        console.log("Registry Size:", this.registry.size);
        console.log();
        console.log("===== REGISTERED AGENTS =====");
        for (const agent of this.registry.getAll()) {
            console.log("Agent ID:", agent.id);
            console.log("Agent Name:", agent.name);
            console.log("Capabilities:");
            console.dir(agent.definition.capabilities, { depth: 10 });
            console.log(`supports("${command}") =`, agent.supports(command));
            console.log("--------------------------------");
        }
        console.log("===============================");
        console.log();
        //
        // 1. Search by Agent ID
        //
        const byId = this.registry.get(normalized);
        console.log("Search by ID:", byId?.id ?? "<NOT FOUND>");
        if (byId) {
            return byId;
        }
        //
        // 2. Search by Agent Name
        //
        const byName = this.registry.findByName(command);
        console.log("Search by Name:", byName?.name ?? "<NOT FOUND>");
        if (byName) {
            return byName;
        }
        //
        // 3. Search by Capability
        //
        const byCapability = this.registry.findByCapability(command);
        console.log("Capability Matches:", byCapability.length);
        if (byCapability.length > 0) {
            console.log("Selected:", byCapability[0].name);
            return byCapability[0];
        }
        console.log("No agent matched.");
        return undefined;
    }
    async execute(context, request) {
        if (this.state === ManagerState.Idle) {
            await this.initialize();
        }
        this.state = ManagerState.Running;
        try {
            return await this.runtime.execute(context, request);
        }
        finally {
            this.state = ManagerState.Ready;
        }
    }
    getState() {
        return this.state;
    }
    getRegistry() {
        return this.registry;
    }
    getRuntime() {
        return this.runtime;
    }
}
//# sourceMappingURL=AgentManager.js.map